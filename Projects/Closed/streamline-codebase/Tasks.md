# streamline-codebase - Tasks

- [x] Delete GUI and Web Packages: `packages/desktop` and `packages/web`.
- [x] Strip remote providers: Remove all providers from `packages/epoch/src/provider` except local and core files.
- [x] Kill telemetry and sharing: Delete `packages/epoch/src/share` and any telemetry logic.