# Epoch Context

**Current Phase:** Implementation Planning

## Active Focus
*   Implementing Task 2: Refactor PromptPayload to ZoneStructuredPayload

## Open Questions / Uncertainties
*   1. How should we handle the 'zone3_active_cursor' specifically? Currently, the Local Cursor Context isn't explicitly passed down into the PromptPayload from the editor extension in a clear way, or at least we need to identify where that cursor data currently lives before we can inject it into zone3. 2. In prompt.ts, zone1 is constructed locally and then prepended to the final instructions array (which contains the joined PromptBuilder.build output). We need to clarify if prompt.ts should continue maintaining its own zone1 or if we should push env and skills directly into payload.zone1 back in llm.ts.

## Pending Intentions
*   Update PromptPayload type, modify llm.ts to push sections to the correct zones, and move construction logic

